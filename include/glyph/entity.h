// entity.h — re-exports scene.h.
// Entity and Scene are tightly coupled (Entity holds a Scene*) and defined
// together in scene.h. This header exists for spec compatibility.
#pragma once
#include <glyph/scene.h>
