<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- OpenGL ES 3.0 required (GLES3 maps to WebGL2 on web, direct on Android) -->
    <uses-feature android:glEsVersion="0x00030000" android:required="true" />

    <!-- Touchscreen is optional so the app installs on non-touch devices (e.g. emulator) -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.gamepad"     android:required="false" />

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:label="@string/app_name"
        android:hardwareAccelerated="true"
        android:allowBackup="false"
        android:theme="@style/AppTheme">

        <!--
          SDLActivity is the SDL3-provided Java bootstrap that:
            1. Loads libmain.so via System.loadLibrary("main")
            2. Creates an EGL surface and forwards lifecycle events to SDL_App*
          No subclassing needed — our C++ callbacks (SDL_AppInit etc.) are the game.
        -->
        <activity
            android:name="org.libsdl.app.SDLActivity"
            android:label="@string/app_name"
            android:alwaysRetainTaskState="true"
            android:launchMode="singleInstance"
            android:configChanges="layoutDirection|locale|orientation|uiMode|screenLayout|screenSize|smallestScreenSize|keyboard|keyboardHidden|navigation"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
